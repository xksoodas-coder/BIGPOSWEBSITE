# بيكسل ميتا (فيسبوك / إنستغرام) — لكل متجر على حدة

كل متجر يضع **معرّف البيكسل الخاص به** بنفسه من لوحة الإدارة. لا يوجد أي معرّف
مشترك، ولا يُحمَّل أي كود من ميتا في متجر لم يضبط بيكسلاً.

---

## للتاجر: 3 خطوات

1. **إنشاء البيكسل**: [Meta Events Manager](https://business.facebook.com/events_manager2)
   ← «ربط مصادر البيانات» ← **ويب** ← أنشئ بيكسلاً وسمِّه باسم المتجر.
   انسخ **معرّف البيكسل** (رقم من 15–16 خانة).
2. **لصقه في المتجر**: لوحة الإدارة ← **إعدادات الموقع** ← بطاقة
   «بيكسل ميتا» ← الصق الرقم ← **حفظ الإعدادات**.
3. **التحقّق**: ثبّت إضافة **Meta Pixel Helper** في كروم، ثم افتح متجرك: يجب أن
   تظهر أحداث `PageView` و`ViewContent`. وفي Events Manager ← **Test Events**
   تظهر الأحداث مباشرةً أثناء تصفّحك.

بعدها، عند إنشاء حملة إعلانية اختر هدف **المبيعات (Conversions)** وحدث
**Purchase**، ليتعلّم فيسبوك استهداف من يشترون فعلاً.

> **مهم مع الدفع عند الاستلام**: يُسجَّل حدث «الشراء» لحظة إرسال الطلب من
> الموقع، لا عند التسليم. لذلك أرقام ميتا تكون أعلى من المبيعات المؤكَّدة بمقدار
> الطلبات الملغاة — قارنها دائماً بنسبة التأكيد لديك.

---

## الأحداث التي يرسلها الموقع

| الحدث | متى | البيانات |
|---|---|---|
| `PageView` | فتح أي صفحة من المتجر | — |
| `ViewContent` | فتح صفحة منتج أو صفحة الطلب المباشر (وعند التنقّل بين المنتجات داخلها) | معرّف المنتج، اسمه، تصنيفه، سعره |
| `AddToCart` | الضغط على زر السلة | المنتج، الكمية، القيمة |
| `InitiateCheckout` | الضغط على «إتمام الطلب»، أو أول كتابة في نموذج الطلب المباشر | محتويات السلة، عدد القطع، القيمة |
| `Purchase` | قبول الخادم للطلب | المحتويات، عدد القطع، القيمة، `eventID` = رقم الطلب |

### نوعا الزبون

للمتجر مساران: **زائر عابر** يصل من رابط أو إعلان فيطلب بملء معلوماته، و**زبون
مسجَّل** له حساب (تاجر جملة عادةً) لا يأتي من الإعلانات. كلاهما متتبَّع، وكل حدث
يحمل `customer_type` بقيمة `guest` أو `registered` لفرز التقارير.

من لا يريد أن تُحسب مبيعات الجملة ضمن نتائج الحملة يُفعّل **«استثناء الزبائن
المسجَّلين من التتبّع»**: عندها لا يُحمَّل كود ميتا إطلاقًا لمن سجّل دخوله. الخيار
**مطفأ افتراضيًا** كي لا يفقد متجر قائم تتبّعه بعد التحديث — وانتبه أن المتجر
المغلق (`orderMode: cart`) يتصفّحه الجميع بعد تسجيل الدخول، ففي هذه الحالة
التفعيل يوقف التتبّع عمليًا عن كل الطلبات.

- العملة `DZD`، وقيمة الشراء = **ثمن المنتجات بلا سعر التوصيل** (كي يعبّر العائد
  على الإعلان عن المبيعات لا عن رسوم الشحن).
- `Purchase` لا يتكرّر لنفس الطلب حتى لو أعاد الزبون تحميل الصفحة (يُخزَّن رقم
  الطلب في `sessionStorage`).

### المطابقة المتقدّمة (اختيارية، معطَّلة افتراضياً)

عند تفعيلها يُرسَل اسم الزبون ورقم هاتفه **مع حدث الشراء فقط**، ويقوم كود ميتا
بتشفيرهما (SHA-256) داخل متصفّح الزبون قبل الإرسال. الفائدة: نسبة مطابقة أعلى
بين الطلب والحساب المعلن له. الرقم يُطبَّع إلى الصيغة الدولية (`0X…` ← `213X…`).
فعّلها فقط إن كان ذلك متوافقاً مع سياسة الخصوصية التي يلتزم بها المتجر.

---

## للمطوّر: أين يقع كل شيء

| الملف | الدور |
|---|---|
| [pixel.js](pixel.js) | كل منطق البيكسل: تحميل SDK ميتا عند الحاجة، تهيئة، طابور أحداث، تعقيم، منع تكرار الشراء |
| [data.js](data.js) | `metaPixelId` / `metaAdvancedMatching` ضمن إعدادات المتجر + تعقيمهما في `getSettings()` |
| [admin-settings.html](admin-settings.html) + [admin.js](admin.js) | بطاقة الإدخال في لوحة الإدارة + التحقّق قبل الحفظ |
| [api/site-settings.js](api/site-settings.js) | تحقّق الخادم من المعرّف (أرقام فقط) قبل التخزين |
| [main.js](main.js) / [order.js](order.js) / [product.js](product.js) | مواضع إطلاق الأحداث |

**التخزين**: `bws_site_settings.settings_json.metaPixelId` لكل `store_id` — لا
جدول جديد ولا هجرة قاعدة بيانات.

**كيف يصل المعرّف إلى الزائر العابر**: `/api/bootstrap` يُعيد `settings` لكل
زائر (قبل تسجيل الدخول) لأن وضع «الطلب المباشر» يحتاجها أصلاً، لذلك يعمل البيكسل
للزوّار المجهولين — وهم أهم من يُقاس في الإعلانات.

**الأمان**: المعرّف هو القيمة الوحيدة القادمة من قاعدة البيانات التي تصل إلى كود
ميتا، لذلك يُفحَص بـ `/^\d{5,20}$/` في ثلاثة مواضع (نموذج الإدارة، الخادم،
والمتصفح قبل `fbq`) ولا يُحقن أبداً داخل HTML.

**الاستدعاءات في الواجهة** — كلها آمنة عند غياب الملف (`window.BWSPixel?.…`):

```js
window.BWSPixel?.refresh();                       // بعد وصول إعدادات المتجر
window.BWSPixel?.viewContent(product);
window.BWSPixel?.addToCart(product, qty);
window.BWSPixel?.initiateCheckoutOnce(items, value);
window.BWSPixel?.purchase(items, value, { orderId, name, phone });
window.BWSPixel?.track('Search', { search_string: q });   // منفذ عام
```

### التطوير اللاحق: Conversions API

حدث `Purchase` يحمل `eventID` = رقم الطلب (uuid). إذا أُرسل الحدث نفسه لاحقاً من
الخادم (عند إنشاء الطلب في `api/orders.js`) بنفس `event_id`، تدمج ميتا النسختين
بلا احتساب مزدوج — وهذا يستعيد المبيعات التي يحجبها مانع الإعلانات أو iOS.
يتطلّب ذلك رمز وصول (Access Token) لكل متجر، وهو غير مُنفَّذ حالياً.
